#!/bin/sh
set -e

if command -v systemctl >/dev/null 2>&1; then
  systemctl daemon-reload || true
fi

case "$1" in
  remove|purge)
    # User requested full folder removal on uninstall.
    rm -rf /opt/NextGenz || true
    ;;
esac

exit 0
